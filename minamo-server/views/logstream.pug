extends _base

block vars
  - var pageTitle = 'log stream'

block links
  link(rel='prerender', href='/console')

block scripts
  script(src='https://cdn.socket.io/socket.io-1.4.5.js', defer)
  script(src='/js/logstream.js', defer)

block header-nav
  li: a(href='/console') console
  li: a.active(href='/logstream') log stream

block content
  h2 Log Stream
  #log
