general:
  build_dir: minamo-server
machine:
  timezone:
    Asia/Tokyo
  node:
    version: 8.1.0
dependencies:
  cache_directories:
    - node_modules
  override:
    - npm install
compile:
  override:
    - npm run build
test:
  override:
    - npm run eslint
